version : '3.9'
services :
  server :
    build :
      context : .
      dockerfile : server/Dockerfile
    container_name: server
    ports :
      - "5555:5555"

  client :
    build :
      context : .
      dockerfile: game/Dockerfile
    container_name : client
    depends_on:
      - server